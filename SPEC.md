# dsh-session-manager 功能规格（应实现的行为）

本文件是插件「应该做成什么样」的契约，作为后续修改与回归测试的依据。
安装见 README.md；当前缺陷与根因见 BUGS.md。

## 1. 回退（Rewind）——功能有且仅有「按轨迹定位用户输入并重建分支」

### 1.1 功能边界

- 只做两件事：
  1. **枚举**当前会话轨迹上的全部用户输入，供用户挑选回退点；
  2. **重建分支**：以所选输入为锚点 `sessions.fork(...)` 生成新会话并打开。
- 不做：文件回退、影子 git、工作区变更检测、SSE 保存状态——0.1.2 下该链路已整体删除。

### 1.2 回退列表的数据来源（核心约束）

- **列表必须覆盖会话的全部用户输入，不依赖聊天窗口当前加载到哪。**
  长会话中用户未滚动/未「加载更早」过的早期轮次，也必须出现在列表里
  （这是 BUGS.md S1 的目标行为）。
- 条目内容：用户消息文本（纯文本拼接；纯图片消息显示占位文案）、所在轮次
  （turn 号）、时间；列表按新 → 旧排序。
- **排除项**：steering 插话消息（聊天投影中为独立 `steering` kind，不属于
  `user`）；合成的 `user/message` 事件（如 `<system-reminder>` 包装的技能/
  上下文注入，见 lab 会话日志 seq 8 实例）——它们不是用户输入，绝不能成为回退点。
- **压缩底线（BUGS.md S8）**：有压缩的会话不能回退到压缩之前 —— 最后一个
  `compaction/end` 之前的真实输入一律不进入列表；压缩后第一条输入仍可回退
  （回到压缩点重新开始），其 fork 边界 = 压缩底线。
- 第一条用户输入不出现在列表中（回退到它之前 = 空会话，无意义）；
  有压缩时该约束被压缩底线取代（底线即最早的回退边界）。

### 1.3 fork 边界语义（0.1.5）

- 0.1.5 的 `sessions.fork({ sessionId, atSeq, increaseTitle: true })` 直接按
  事件 seq 切种子（0.1.2 的「向前吸附到 turn/end」已移除）；边界必须是已存在
  的事件 seq，且切片不得停在未闭合的 turn（`_forkSeed` 校验）。
- 每条回退点自带边界（服务端 `inputs[].boundary`）：无压缩时 = 上一条用户
  输入的 seq（回退到第 k 条 = 撤销第 k 条，种子含第 k-1 条及其之前全部）；
  有压缩时压缩后首条 = 压缩底线（最后一个 `compaction/end` 的 seq，种子完整
  携带压缩段，子会话投影重放时折叠回检查点状态）。
- fork 成功后：把被选消息文本放回新会话输入框草稿，打开新会话；并清空 fork
  子会话经种子回放继承的父会话排队消息（BUGS.md S7，`rewind/drain`）——
  否则新回合按 FIFO 认领队头，会把陈旧排队消息当成输入发出。

### 1.4 交互

- 入口：会话头部「回退」按钮；快捷键 `Ctrl/Cmd+Shift+Z`（作用于当前会话）。
- 弹窗：列表单选 → 确认回退；确认中禁用操作；失败在弹窗内显示错误。
- 加载列表允许短暂的加载态，但**不得要求用户先手动滚动/加载历史**。

## 2. 归档管理（Archives）

标准通道命令共 5 个：`archives-list` / `archives-unarchive` / `archives-delete` /
`archives-delete-all` / `archives-delete-ungrouped`。UI（侧边栏入口）提供：
恢复到工作区、单个删除（连带日志文件，二次确认）、删除全部、删除全部未分组、
按标题/工作区/时间/大小/状态排序；运行中会话禁止删除；文件缺失的归档禁止恢复。

## 3. 验收纪律

- 语法通过 ≠ 功能正确。修改后必须在浏览器实际操作（现有 Chrome +
  browser-harness）：长会话打开回退弹窗不滚动 → 应看到全部用户输入；
  选早期输入回退 → 新分支从该输入前重新开始、草稿回填、原文会话不动。
- 插件 client.js / 服务端代码变更后须重启 dsh web 实例（composition 缓存）。
